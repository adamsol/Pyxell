
func f(t: Int*String*Bool, d: Int): Bool*Int*String def
    if d > 0 do
        t = t.a - 1, t.b, not t.c
        return f(t, d-1)
    print t
    return not t.c, t.a + 42, t.b + "#$"

print f((12, "!@", false), 50)
